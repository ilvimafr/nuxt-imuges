type User {
  id:     ID!
  name:   String!
  images: [Image]
}
type Image {
  id:        ID!
  createdAt: String!
  published: Boolean!
  author:    User!
  authorID:  ID!
}

type Query {
  getUser(id: ID!): User
}

type Mutation {
  changeUserName(name: String!): Boolean
}